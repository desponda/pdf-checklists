{
  "buildCommand": "npm --prefix apps/frontend run build --workspaces=false",
  "installCommand": "npm ci",
  "outputDirectory": "apps/frontend/dist",
  "functions": {
    "api/**/*.js": {
      "maxDuration": 60
    }
  }
}
